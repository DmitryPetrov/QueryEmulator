удалить метод interceptor() в com.emulator.config.Config

StatementRequestData getters/setters for requestAttrRequestId and requestAttrVersion

сделать Incoming.docType как enum

RequestParameters throws декларации

сделать переменную в application.properties, которая будет включать и выключать
        печать soap сообщений в sout

вынести создание ДТО в функции

в контроллерах бросать ексепшен если не найден request chain
