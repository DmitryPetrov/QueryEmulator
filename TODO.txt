удалить метод interceptor() в com.emulator.config.Config

StatementRequestData getters/setters for requestAttrRequestId and requestAttrVersion

сделать Incoming.docType как enum

сделать переменную в application.properties, которая будет включать и выключать
        печать soap сообщений в sout

автоматическое увеличение requestID incomingId

RequestChain setGetRequestStatus()
        this.statementRequestStatus = dto.getStateResponseList().get(0).getState()

com.emulator.domain.soap.requests.getrequeststatus.ResponseHandler getNodeValue()
        Node node = doc.getElementsByTagName(nodeName).item(0);

сделать список строк из ошибок от сервера и прогонять через него каждый ответ на поиск ошибок

добавить пакеты data в пакеты запросов

решить что делать с entity

